# yaml-language-server: $schema=https://devenv.sh/devenv.schema.json
inputs:
  autocommitmsg:
    url: github:sestrella/autocommitmsg
    overlays: [default]
  git-hooks:
    url: github:sestrella/git-hooks.nix/add_gitleaks_hook
  nixpkgs:
    url: github:cachix/devenv-nixpkgs/rolling

secretspec:
  enable: true
  provider: keyring
  profile: development
