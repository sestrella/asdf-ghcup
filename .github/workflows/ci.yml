---
name: build

on: push

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  unit:
    strategy:
      matrix:
        runner:
          - macos-latest
          - ubuntu-latest
      fail-fast: true
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: true
      - name: Remove GHCup
        run: ghcup nuke
      - name: Check GHCup is not present
        run: not command -v ghcup
        shell: bash
      - name: Run tests
        run: ./test/bats/bin/bats -T test

  integration:
    strategy:
      matrix:
        runner:
          - macos-latest
          - ubuntu-latest
      fail-fast: true
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Remove GHCup
        run: ghcup nuke
      - name: Check GHCup is not present
        run: not command -v ghcup
        shell: bash
      - name: Test plugin
        uses: asdf-vm/actions/plugin-test@v3
        with:
          plugin: ghc
          command: ghc --version
